package ps.assignment.repositories;

import ps.assignment.models.FxDeal;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FxDealRepository extends JpaRepository<FxDeal, String> {
}

